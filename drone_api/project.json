{
  "name": "drone_api",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "drone_api/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "drone_api:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "drone_api:build:development"
        },
        "production": {
          "buildTarget": "drone_api:build:production"
        }
      }
    }
  }
}
